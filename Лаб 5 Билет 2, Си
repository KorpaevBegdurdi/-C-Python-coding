#include <stdio.h>

int main() {
    int n, sum = 0, i = 1;

    // Вводим значение n
    printf("Введите значение n: ");
    scanf("%d", &n);

    
    while (i <= n) {
        sum += i;  
        i++;      
    }

    
    printf("Сумма чисел от 1 до %d равна %d\n", n, sum);

    return 0;
}
